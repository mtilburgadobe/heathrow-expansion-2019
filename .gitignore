.hlx/*
coverage/*
logs/*
node_modules/*

helix-importer-ui
.DS_Store
*.bak
.idea

# Ensure these files are tracked
!CLAUDE.md
!AGENTS.md
# Snowflake skill: in-progress run state (committed only on close)
.snowflake/projects/*/state.json
# Local Claude Code skills (not project artifacts)
.claude/
# Snowflake: captured source images (uploaded to da-media, not vendored in git)
.snowflake/projects/*/input/*.png
.snowflake/projects/*/input/*.jpg
.snowflake/projects/*/input/*.gif
.snowflake/projects/*/input/*.webp
